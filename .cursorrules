# Cursor 配置规则

## Git Commit 消息生成规则
- 所有 git commit 消息必须使用中文编写
- 必须遵循 Conventional Commits 标准格式

### Commit 消息格式规范
```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型（必填）
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档变更
- `style`: 代码格式调整（不影响代码运行）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动
- `ci`: CI 配置文件和脚本的变更
- `build`: 构建系统或外部依赖的变更

### Scope 作用域（可选）
- 指定变更影响的范围，如：`user`, `order`, `payment`, `api` 等
- 如果变更影响多个模块，可以省略 scope

### Subject 主题（必填）
- 使用中文简洁描述变更内容
- 首字母小写，结尾不加句号
- 使用祈使句，如："添加用户登录功能" 而不是 "添加了用户登录功能"

### Body 正文（可选）
- 详细说明变更的动机和实现方式
- 与 subject 之间空一行
- 每行不超过 72 个字符

### Footer 脚注（可选）
- 用于说明破坏性变更（BREAKING CHANGE）
- 关联 issue（如：`Closes #123`）

### 示例
```
feat(user): 添加用户登录功能

实现了基于 JWT 的用户认证机制，支持用户名和密码登录

Closes #123
```

```
fix(order): 修复订单状态更新异常

修复了订单状态在某些情况下无法正确更新的问题
```

```
docs: 更新 API 文档

更新了用户服务相关的 API 接口文档
```

## 代码生成规则
- 代码注释使用中文
- 变量和函数命名遵循项目规范（英文命名）
- 错误消息和日志信息使用中文

